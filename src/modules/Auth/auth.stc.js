import styled from 'styled-components';

export const FormWithLogoStc = styled.div`
    width: 500px;
    padding: 5rem;
    margin-top: 1rem;
    background: white;
    border-radius: 20px;
`;

export const FooterStc = styled.div`
    text-align: center;
    margin-top: ${({ gap }) => `${gap}px`};
`;
